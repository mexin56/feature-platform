import { Typography } from 'antd'

export default function Runs() {
  return (
    <div>
      <Typography.Title level={4}>实例</Typography.Title>
      <Typography.Text type="secondary">工作流实例列表 — 即将实现</Typography.Text>
    </div>
  )
}
