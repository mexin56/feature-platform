import { Typography } from 'antd'

export default function Admin() {
  return (
    <div>
      <Typography.Title level={4}>系统管理</Typography.Title>
      <Typography.Text type="secondary">用户/连接/API Key/Webhook — 即将实现</Typography.Text>
    </div>
  )
}
