import { Typography } from 'antd'

export default function FeatureGroups() {
  return (
    <div>
      <Typography.Title level={4}>特征组</Typography.Title>
      <Typography.Text type="secondary">特征组列表 — 即将实现</Typography.Text>
    </div>
  )
}
