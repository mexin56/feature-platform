import { Typography } from 'antd'

export default function Workflows() {
  return (
    <div>
      <Typography.Title level={4}>工作流</Typography.Title>
      <Typography.Text type="secondary">工作流列表 — 即将实现</Typography.Text>
    </div>
  )
}
